"""Pytest configuration.

Its presence at the project root puts the root on ``sys.path`` so tests can
import the package as ``src.snake_ai...`` without an editable install.
Headless by default: tests use ``render=False`` so no display is required.
"""
